package com.dynamite.shopping.pageobjects.details;

import com.dynamite.shopping.page.Page;
import org.openqa.selenium.WebDriver;

public class DetailsPage extends Page {

    public DetailsPage(WebDriver driver) {
        super(driver);
    }
}
