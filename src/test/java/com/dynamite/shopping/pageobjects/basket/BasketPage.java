package com.dynamite.shopping.pageobjects.basket;

import com.dynamite.shopping.page.Page;
import org.openqa.selenium.WebDriver;

public class BasketPage extends Page {

    public BasketPage(WebDriver driver) {
        super(driver);
    }
}
