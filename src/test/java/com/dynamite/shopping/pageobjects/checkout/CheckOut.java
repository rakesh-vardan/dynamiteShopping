package com.dynamite.shopping.pageobjects.checkout;

import com.dynamite.shopping.page.Page;
import org.openqa.selenium.WebDriver;

public class CheckOut extends Page {

    public CheckOut(WebDriver driver) {
        super(driver);
    }
}
